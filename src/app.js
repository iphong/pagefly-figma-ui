import React from 'react';
import ReactDOM from 'react-dom';
import { Layout } from './views/Layout';
ReactDOM.render(React.createElement(Layout, null), document.body.appendChild(document.createElement('main')));
